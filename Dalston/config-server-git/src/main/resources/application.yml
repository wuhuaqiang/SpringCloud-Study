spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yihonglei/SpringCloud-Learning.git/
          search-paths: Dalston/config-repo
#          username:
#          password:

server:
  port: 1201